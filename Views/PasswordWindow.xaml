<Window x:Class="HMI_ScrewingMonitor.Views.PasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xác thực" Height="180" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/ColorTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="White" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nhập mật khẩu truy cập" 
                   FontSize="14" Margin="0,0,0,10"
                   Foreground="Black"/>

        <PasswordBox x:Name="PasswordInput" Grid.Row="1" 
                     Height="30" Margin="0,0,0,20"
                     VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Xác nhận" Click="OkButton_Click" 
                    Width="100" Height="30" Margin="5,0"
                    IsDefault="True"/>
            <Button Content="Hủy" Click="CancelButton_Click"
                    Width="100" Height="30" Margin="5,0"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
